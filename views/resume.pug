extends layout.pug

block content

  nav.nav
    a(href='/')
      span &larr;
      |  Back

  article.portfolio-about
    .inner-wrapper.flex-row-wrap.two-col
      .about-intro-box.box
        h1 My resume

        .about-bio

          embed(src='/static/images/resume2.pdf' type='application/pdf' width='100%' height='800px')

    nav.nav
      a(href='/')
        span &larr;
        |  Back